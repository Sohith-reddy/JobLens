# JobLens AI - Job Scam Detection & Resume Matching
# Python 3.9+ required

# Core ML dependencies
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# Environment variables
python-dotenv>=1.0.0

# API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6  # For file uploads

# HTTP client (async)
httpx>=0.25.0
brotli>=1.1.0  # For Brotli-compressed responses

# HTML parsing and extraction
beautifulsoup4>=4.12.0
lxml>=4.9.0
readability-lxml>=0.8.1

# PDF parsing
PyMuPDF>=1.23.0  # Primary PDF extraction (fitz)
pdfminer.six>=20221105  # Fallback PDF extraction

# Browser automation for JS-heavy sites and 403-blocked sites
# After installing, run: playwright install chromium
playwright>=1.40.0